name: almalinux
type: base
platforms:
  - amd64
tags:
  - ${OS_VERSION}
  - ${OS_VERSION}-${COMMIT_SHA_SHORT}
  - ${OS_VERSION}-${BUILD_TIMESTAMP}
  - ${OS_VERSION}-${BUILD_TIMESTAMP}-${COMMIT_SHA_SHORT}
os_versions:
  - 9
